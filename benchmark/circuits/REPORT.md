# Discovered refuse/answer safety circuit

Automated circuit discovery (ACDC; Conmy et al., NeurIPS 2023) over a transparent
safety circuit, using path patching (Goldowsky-Dill et al., 2023) as the causal
primitive. Generated by `verigrad circuit`; do not edit by hand.

- **Edges kept:** 8 of 13 (sparsity 38%)
- **Faithfulness:** KL(clean ‖ circuit-only) = 0.0087 nats (lower = the circuit reproduces the behavior)
- **Threshold:** tau = 0.02 nats per edge

## Discovered circuit (kept edges)

| parent | → | child |
|---|---|---|
| `benign` | → | `guard` |
| `guard` | → | `answer_logit` |
| `guard` | → | `refuse_logit` |
| `harm` | → | `threat` |
| `jailbreak` | → | `threat` |
| `threat` | → | `guard` |
| `topic` | → | `benign` |
| `topic` | → | `threat` |

## Pruned edges

- `benign` → `answer_logit`
- `benign` → `refuse_logit`
- `harm` → `benign`
- `noise` → `refuse_logit`
- `refusal_cue` → `guard`
